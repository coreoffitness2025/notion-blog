# 작업 일지 - 2026년 2월 22일

## 개요
Team 페이지 리뉴얼 — missionSteps 4단계 제거, Mission & Vision 중심 단순화 + 스타일 반복 조정

## 완료된 작업

### 1. Team 페이지 구조 리뉴얼
**요청**: missionSteps 01~04 제품 기능 나열 제거. Mission + Vision 두 블록으로 단순화
**해결**:
- i18n: `missionTitle`, `missionVision`, `missionDesc`, `missionSteps` 삭제
- i18n: `missionLabel`, `missionText`, `visionLabel`, `visionHeadline`, `visionText`, `closing`, `teamLabel` 추가
- ko/en 양쪽 동기화
- client.tsx: Hero → Mission → Vision → Team → Contact CTA 4섹션 구조

**수정 파일**:
- src/lib/dictionaries/ko.ts
- src/lib/dictionaries/en.ts
- src/app/[locale]/team/client.tsx

### 2. 스타일 반복 조정 (5회)
**조정 내역**:
1. Mission/Vision 라벨 색상: `text-gray-400` → `text-[var(--corevia-primary)]`
2. 하드코딩 `#4285F4` → CSS 변수 `var(--corevia-primary)` 통일
3. 글씨 크기 확대: 라벨 text-xs → 45px, 본문 text-xl → 24px
4. font-bold → font-normal/font-medium으로 경량화
5. MISSION/VISION/TEAM 3섹션 독립 분리 (`border-t` + `py-32 md:py-40`)

### 3. 최종 페이지 구조
```
Section 1: Hero          — "Team CoreVia" + subtitle
Section 2: Mission       — 45px 라벨 + 24px Core/Via 파란색 본문
Section 3: Vision        — 45px 라벨 + 24px 헤드라인 + 24px 본문
Section 4: Team          — 45px 라벨 + closing + James 프로필
Section 5: Contact CTA   — 문의 버튼
```

## 기술적 세부사항
- `border-t border-gray-100`으로 섹션 구분
- Vision 본문 `\n` → `split("\n")` + `<br />` 줄바꿈 처리
- Core/Via regex split 하이라이트 유지
- CSS 변수 `var(--corevia-primary)` 일관 사용

## 커밋 이력 (6개)
- `2814a7c` refactor(team): Mission & Vision 중심 단순화 — missionSteps 제거
- `fa9652b` style(team): Mission/Vision 라벨 강조 + primary color 변수 통일
- `945ba21` style(team): Mission/Vision 글씨 크기 대폭 확대
- `18d82ad` style(team): 라벨 45px + 본문 30px 크기 조정
- `e772fde` style(team): 본문 font-normal 24px + AI Native Fitness 본문 크기 통일
- `6de40be` refactor(team): MISSION/VISION/TEAM 3섹션 분리 + TEAM 라벨 추가

## 변경된 파일 목록
- src/app/[locale]/team/client.tsx
- src/lib/dictionaries/ko.ts
- src/lib/dictionaries/en.ts
